import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ProductRefuseComponent } from './product-refuse.component';

describe('ProductRefuseComponent', () => {
  let component: ProductRefuseComponent;
  let fixture: ComponentFixture<ProductRefuseComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ProductRefuseComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ProductRefuseComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
